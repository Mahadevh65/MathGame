# Not currently required — the Vite dev server proxies /api to
# http://localhost:8080 (see vite.config.ts). Add VITE_API_URL here if you
# later point the frontend at a deployed backend instead of the dev proxy.
VITE_API_URL=http://localhost:8080/api
